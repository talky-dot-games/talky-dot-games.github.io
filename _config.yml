# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Your awesome title
email: your-email@example.com
description: >- # this means to ignore newlines until "baseurl:"
  test
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: jekyllrb
github_username:  jekyll

# Build settings
theme: minima
plugins:
  - jekyll-feed

defaults:
  - scope:
      path: "assets/images/photos"
    values:
      phot: 0
  - scope:
      path: "assets/images/photos/1"
    values:
      phot: 1
      postD: August 15, 2024 
  - scope:
      path: "assets/images/photos/2"
    values:
      phot: 2
      postD: June 9, 2024
  - scope:
      path: "assets/images/photos/3"
    values:
      phot: 3
      postD: December 21, 2023
  - scope:
      path: "assets/images/photos/4"
    values:
      phot: 4
      postD: November 30, 2023
  - scope:
      path: "assets/images/photos/5"
    values:
      phot: 5
      postD: October 9, 2023
  - scope:
      path: "assets/images/photos/6"
    values:
      phot: 6
      postD: August 12, 2023
  - scope:
      path: "assets/images/photos/7"
    values:
      phot: 7
      postD: May 19, 2023
  - scope:
      path: "assets/images/photos/8"
    values:
      phot: 8
      postD: May 2, 2023
  - scope:
      path: "assets/images/photos/9"
    values:
      phot: 9
      postD: March 26, 2023
  - scope:
      path: "assets/images/photos/10"
    values:
      phot: 10
      postD: March 9, 2023
  - scope:
      path: "assets/images/photos/11"
    values:
      phot: 11
      postD: February 19, 2023
  - scope:
      path: "assets/images/photos/12"
    values:
      phot: 12
      postD: October 29, 2022
  - scope:
      path: "assets/images/art"
    values:
      art: true
  - scope:
      path: "assets/images/pixels"
    values:
      pixel: true
      

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
